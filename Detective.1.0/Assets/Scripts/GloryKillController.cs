using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GloryKillController : MonoBehaviour
{
    /*
     * This should be attached to main player object (the parent)
     * The script should take care of all events
     * AFTER a glory kill has been started.
     * All events before should be taken care of by a player function.
     * 
     */

    //Function that moves the player to the correct position to start 

    //Function that moves the robot and player in junction with the animation

}
